{
	"name": "game",
	"version": "1.0.0",
	"description": "Game",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"start": "run-p start-compile start-run",
		"start-compile": "tsc --watch",
		"start-run": "browser-sync start --server --files=\"build/*\""
	},
	"devDependencies": {
		"@types/p5": "^1.3.1",
		"@types/socket.io-client": "^3.0.0",
		"browser-sync": "^2.27.7",
		"npm-run-all": "^4.1.5",
		"typescript": "^4.5.2"
	},
	"dependencies": {}
}
